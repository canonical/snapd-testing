name: metrics-exporter
summary: Install prometheus-pushgateway snap

description: |
  Machine charm that installs prometheus-pushgateway snap
  used to collect custom metrics on port 9091.

type: charm
base: ubuntu@24.04
platforms:
  amd64:

parts:
  charm:
    plugin: charm
    source: .
    charm-requirements:
      - requirements.txt
